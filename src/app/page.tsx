export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 px-6 text-center">
      <h1 className="text-3xl font-semibold text-brand-800">EcoTrack</h1>
      <p className="max-w-md text-base text-brand-700/80">
        Personal carbon footprint tracking is coming together — auth, logging, and
        dashboards land in the next build steps.
      </p>
    </main>
  );
}
